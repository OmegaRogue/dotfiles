- defaults:
    link:
      relink: true
      create: true
    shell:
      stdout: true
      stderr: true

- create:
    #- $HOME/projects
    #- $HOME/Downloads
    - $HOME/.config

- clean:
    $HOME:
      force: true
    $HOME/.config:
      recursive: true

- link:
    # zsh
  
    # antigen
    


    # bash
    ~/.profile: bash/profile
    ~/.bashrc: bash/bashrc
    ~/.bash_logout: bash/bash_logout
    
    # git

    # neovim
    
    # ssh
    
    # scripts
    ~/.scripts: scripts

    # LaTeX
    # ~/.latexmkrc: latexmk/latexmkrc

    # NPM
    # ~/.npmrc: npm/npmrc

    # tmux
    
    # powerline
    #~/.config/powerline: powerline



- shell:
    #- chsh -s  $(which zsh)
    #- sudo chsh -s $(which zsh)
    -
        command: zsh ~/.dotfiles/scripts/zsh_install.sh
        description: Installing Zsh

    -
        command: zsh ~/.dotfiles/scripts/neovim_install.sh
        description: Installing Neovim
  #  -
  #      command: ~/.dotfiles/scripts/dotnet_install.sh
  #      description: Installing DotNet Core SDK
  #  - 
  #      command: zsh ~/.dotfiles/scripts/golang_install.sh
  #      description: Installing Golang
  #      quiet: false
  #      stdout: true
